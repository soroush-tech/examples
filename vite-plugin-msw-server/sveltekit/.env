# Toggles the msw mock server that vite-plugin-msw-server starts.
# On by default so the demo works out of the box. Set to false — or override in the shell,
# e.g. `MSW_ACTIVE=false npm run build` — to make the plugin an inert no-op.
MSW_ACTIVE=false
